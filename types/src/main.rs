fn main() {
    let num1 = 10; //By default rust assumes i32 type

    let num2: u32 = 36; //Type annotattion , specifying type of unsigned 32 integer

    let num3 = 3.6; //By default rust assumes f64 type in case of floating point numbers

    let num4 = 9.5f32; //We can specify the type as a suffix to the value as well
}
